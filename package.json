{
  "name": "princeton-academic-events-api",
  "version": "2.0.0",
  "description": "Cloudflare Worker API for Princeton Academic Events",
  "main": "src/simple-worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:init": "wrangler d1 execute princeton-academic-events-db --file=schema.sql",
    "db:local": "wrangler d1 execute princeton-academic-events-db --local --file=schema.sql",
    "test": "echo \"Tests would go here\""
  },
  "keywords": ["cloudflare", "worker", "api", "princeton", "events"],
  "author": "Princeton Academic Events Team",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}